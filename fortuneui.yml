eureka:
  client:
    shouldRegisterWithEureka: false
hystrix:
  command:
    FortuneService:
      threadPoolKeyOverride: FortuneServiceTPool
  stream:
    validate_certificates: false
fortuneService:
  scheme: http
  address: fortuneservice-undefaming-anticathexis.apps.testcloud.com
  randomFortunePath: api/fortunes/random
  allFortunesPath: api/fortunes/all
